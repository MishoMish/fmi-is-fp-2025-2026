# Въведение в Haskell

## Въведение

Функционалното програмиране (ФП) е парадигма, в която програмите се изграждат чрез **композиция на функции**. За разлика от императивното програмиране (C, Java), тук няма променливи, цикли или странични ефекти — всичко е **изрази**, които се **оценяват** до стойности.

**Haskell** е чисто функционален, статично типизиран, мързеливо оценяващ език.

---

## 1. GHCi — интерактивна среда

### Инсталация

Използвайте [GHCup](https://www.haskell.org/ghcup/) за инсталация на GHC (компилатор) и GHCi (интерактивна среда).

### Стартиране

```bash
ghci
```

GHCi е REPL (Read-Eval-Print Loop) — въвеждате израз, получавате резултат:

```haskell
ghci> 2 + 3
5
ghci> "Hello" ++ " World"
"Hello World"
```

### Зареждане на файл

```bash
ghci> :load MyFile.hs    -- или :l MyFile.hs
ghci> :reload             -- или :r (след промяна на файла)
ghci> :type expression    -- или :t (показва типа)
ghci> :info SomeType      -- или :i (информация за тип)
ghci> :quit               -- или :q (изход)
```

---

## 2. Основни типове

| Тип | Описание | Примери |
|-----|----------|---------|
| `Int` | Цяло число (ограничено) | `42`, `-7`, `0` |
| `Integer` | Цяло число (неограничено) | `123456789012345` |
| `Double` | Число с плаваща точка | `3.14`, `-0.5` |
| `Bool` | Булева стойност | `True`, `False` |
| `Char` | Единичен символ | `'a'`, `'Z'`, `'5'` |
| `String` | Низ (= `[Char]`) | `"Hello"` |

```haskell
ghci> :t 42
42 :: Num a => a        -- полиморфен тип

ghci> :t True
True :: Bool

ghci> :t 'x'
'x' :: Char

ghci> :t "hello"
"hello" :: String       -- същото като [Char]
```

---

## 3. Аритметика и оператори

```haskell
ghci> 2 + 3          -- 5
ghci> 10 - 4         -- 6
ghci> 3 * 7          -- 21
ghci> 10 / 3         -- 3.3333...  (дробно деление)
ghci> 10 `div` 3     -- 3          (целочислено деление)
ghci> 10 `mod` 3     -- 1          (остатък)
ghci> 2 ^ 10         -- 1024       (степенуване)
```

### Оператори за сравнение

```haskell
ghci> 3 == 3    -- True
ghci> 3 /= 4    -- True   (/= е "не е равно", НЕ !=)
ghci> 3 < 5     -- True
ghci> 3 >= 3    -- True
```

### Булеви оператори

```haskell
ghci> True && False   -- False
ghci> True || False   -- True
ghci> not True        -- False
```

---

## 4. Условни изрази

### `if-then-else`

В Haskell `if` е **израз** (връща стойност), не оператор:

```haskell
ghci> if 3 > 2 then "yes" else "no"
"yes"
```

> ⚠️ `else` е **задължителен**! Всеки клон трябва да връща стойност от **един и същ тип**.

```haskell
absolute :: Int -> Int
absolute x = if x >= 0 then x else -x
```

---

## 5. Дефиниране на функции

### Прост синтаксис

```haskell
-- Типова сигнатура (незадължителна, но препоръчителна)
double :: Int -> Int
double x = x * 2

-- Функция с два аргумента
add :: Int -> Int -> Int
add x y = x + y
```

> 💡 Няма скоби около аргументите и няма запетаи между тях! `add(x, y)` е грешно — правилно е `add x y`.

### Извикване

```haskell
ghci> double 5      -- 10
ghci> add 3 4       -- 7
ghci> add (double 2) 3  -- 7  (скоби само за групиране)
```

---

## 6. Типови сигнатури

```haskell
functionName :: ArgType1 -> ArgType2 -> ReturnType
```

Примери:

```haskell
isEven :: Int -> Bool
isEven x = x `mod` 2 == 0

hypotenuse :: Double -> Double -> Double
hypotenuse a b = sqrt (a^2 + b^2)
```

> 💡 `->` е дясно-асоциативен: `Int -> Int -> Int` е `Int -> (Int -> Int)`. Това е свързано с **currying** — ще го разгледаме по-късно.

---

## 7. `let` и `where`

### `where` — локални дефиниции след тялото

```haskell
bmi :: Double -> Double -> String
bmi weight height
  | index < 18.5 = "Поднормено"
  | index < 25.0 = "Нормално"
  | index < 30.0 = "Наднормено"
  | otherwise     = "Затлъстяване"
  where
    index = weight / height ^ 2
```

### `let ... in` — локални дефиниции преди израза

```haskell
cylinderArea :: Double -> Double -> Double
cylinderArea r h =
  let baseArea = pi * r ^ 2
      sideArea = 2 * pi * r * h
  in  2 * baseArea + sideArea
```

---

## 8. Полезни функции от Prelude

| Функция | Тип | Описание |
|---------|-----|----------|
| `succ` | `a -> a` | Следващ (+1) |
| `pred` | `a -> a` | Предишен (-1) |
| `abs` | `Num a => a -> a` | Абсолютна стойност |
| `signum` | `Num a => a -> a` | Знак (-1, 0, 1) |
| `min` | `Ord a => a -> a -> a` | Минимум от две стойности |
| `max` | `Ord a => a -> a -> a` | Максимум от две стойности |
| `even` | `Integral a => a -> Bool` | Четно ли е? |
| `odd` | `Integral a => a -> Bool` | Нечетно ли е? |
| `gcd` | `Integral a => a -> a -> a` | НОД |

---

## Обобщение

| Концепция | Описание |
|-----------|----------|
| GHCi | Интерактивна среда за Haskell |
| Основни типове | `Int`, `Double`, `Bool`, `Char`, `String` |
| `if-then-else` | Условен **израз** (не оператор) |
| Функции | `name arg1 arg2 = body` |
| Типови сигнатури | `name :: Type1 -> Type2 -> RetType` |
| `where` | Локални дефиниции след тялото |
| `let ... in` | Локални дефиниции преди израза |
