# Седмица 1 — Примери

## Пример 1: Аритметика в GHCi

```haskell
ghci> 2 + 3 * 4       -- 14 (приоритет на *)
ghci> (2 + 3) * 4     -- 20
ghci> 7 `div` 2       -- 3
ghci> 7 `mod` 2       -- 1
ghci> sqrt 16          -- 4.0
ghci> max 3 7          -- 7
ghci> min 3 7          -- 3
```

---

## Пример 2: Прости функции

```haskell
-- Знак на число
sign :: Int -> Int
sign x = if x > 0 then 1
         else if x == 0 then 0
         else -1

-- Високосна година
isLeapYear :: Int -> Bool
isLeapYear y = (y `mod` 4 == 0 && y `mod` 100 /= 0) || y `mod` 400 == 0

-- Лице на триъгълник по три страни (формула на Херон)
triangleArea :: Double -> Double -> Double -> Double
triangleArea a b c =
  let s = (a + b + c) / 2
  in  sqrt (s * (s - a) * (s - b) * (s - c))
```

> 💡 Забележете как `let ... in` ни позволява да дефинираме `s` веднъж и да я използваме многократно.

---

## Пример 3: Функции с `where`

```haskell
-- Решения на квадратно уравнение ax² + bx + c = 0
quadraticRoots :: Double -> Double -> Double -> (Double, Double)
quadraticRoots a b c = (x1, x2)
  where
    discriminant = b^2 - 4*a*c
    sqrtD = sqrt discriminant
    x1 = (-b + sqrtD) / (2 * a)
    x2 = (-b - sqrtD) / (2 * a)
```

```haskell
ghci> quadraticRoots 1 (-3) 2
(2.0, 1.0)
```

> ⚠️ Тази функция не проверява дали дискриминантата е отрицателна. Ще научим как да обработваме такива случаи с `Maybe` в Седмица 8.

---

## Пример 4: Оператори като функции и обратно

```haskell
-- Оператор като функция (с обикновени скоби):
ghci> (+) 3 4          -- 7
ghci> (*) 2 5          -- 10

-- Функция като оператор (с обратни кавички):
ghci> 10 `div` 3       -- 3
ghci> 10 `mod` 3       -- 1
ghci> max 3 7           -- 7  (обикновено)
ghci> 3 `max` 7        -- 7  (като оператор)
```

---

## Пример 5: Типове и полиморфизъм

```haskell
ghci> :t (+)
(+) :: Num a => a -> a -> a
-- "За всеки тип a, който е Num, (+) приема два a и връща a"

ghci> :t (==)
(==) :: Eq a => a -> a -> Bool
-- "За всеки тип a, който е Eq, (==) приема два a и връща Bool"

ghci> :t max
max :: Ord a => a -> a -> a
-- "За всеки тип a, който е Ord, max приема два a и връща a"
```

> 💡 `Num a =>`, `Eq a =>`, `Ord a =>` са **ограничения на типови класове** (type class constraints). Ще ги разберем по-подробно в Седмица 9.
