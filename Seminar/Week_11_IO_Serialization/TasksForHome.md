# Седмица 11 - Задачи за домашна работа

## Задача 1: Честотен анализ

Напишете програма, която чете текстов файл и отпечатва:

- Честотата на всяка дума (сортирана по честота, намаляващо)
- Топ 10 най-често срещани думи

```haskell
wordFrequency :: FilePath -> IO ()
```

---

## Задача 2: CSV филтриране

Напишете програма, която:

1. Чете CSV файл
2. Пита потребителя за колона и стойност
3. Филтрира редовете и записва резултата в нов файл

```haskell
filterCSV :: FilePath -> FilePath -> IO ()
```

```
Колона (номер): 2
Стойност: София
Записано 5 реда в output.csv
```

---

## Задача 3: Адресна книга с файлове

Създайте програма за адресна книга, която:

- Поддържа записи `(Име, Телефон, Имейл)`
- Зарежда данните от JSON-подобен текстов файл при стартиране
- Позволява добавяне, търсене, изтриване, редактиране
- Записва данните при изход

Формат на файла:

```
name:Иван;phone:0888123456;email:ivan@mail.bg
name:Мария;phone:0899654321;email:maria@mail.bg
```

---

## Задача 4: Markdown → HTML конвертор

Напишете **прост** Markdown → HTML конвертор, който обработва:

- `# Заглавие` → `<h1>Заглавие</h1>`
- `## Подзаглавие` → `<h2>Подзаглавие</h2>`
- `**bold**` → `<strong>bold</strong>`
- `*italic*` → `<em>italic</em>`
- Празен ред → `<br>`
- Всичко друго → `<p>текст</p>`

```haskell
convertMarkdown :: FilePath -> FilePath -> IO ()
```

---

## Задача 5: Log анализатор

Даден е лог файл с формат:

```
2025-01-15 10:30:00 INFO Server started
2025-01-15 10:31:05 ERROR Connection timeout
2025-01-15 10:32:00 INFO Request processed
2025-01-15 10:33:15 WARN Memory usage high
```

Напишете програма, която:

- Брои съобщенията по ниво (INFO, WARN, ERROR)
- Извлича всички ERROR съобщения
- Филтрира по дата (подадена от потребителя)
- Записва отчет в нов файл

```haskell
analyzeLog :: FilePath -> IO ()
```

---

## 🌟 Бонус: Файлова система симулация

Симулирайте проста файлова система в паметта с IO интерфейс:

- `mkdir <path>` - създава директория
- `touch <path>` - създава файл
- `ls <path>` - списък на съдържанието
- `cat <path>` - показва съдържание
- `write <path> <text>` - записва текст
- `tree` - показва цялото дърво

Файловата система се пази в `IORef` (или като аргумент на рекурсивна функция).
